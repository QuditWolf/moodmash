# VibeGraph Frontend Environment Configuration for Docker
# This file is used when running the frontend in a Docker container

# API Base URL - Points to the backend API container
# The backend-api service name is resolved by Docker's internal DNS
VITE_VIBEGRAPH_API_URL=http://backend-api:8000

# Environment
VITE_ENV=development

# Node Environment
NODE_ENV=development
